# KPP Agri Dashboard v3.0.0

รุ่นปรับปรุงตามแนวทางรายงานสถานการณ์การเพาะปลูกใน Power BI

## สิ่งที่ปรับปรุง

- รวมรายงานพืช 9 หน้าเป็น Dashboard เดียวและเปลี่ยนพืชด้วยตัวกรอง
- เพิ่ม KPI เทียบกับปีก่อนสำหรับพื้นที่เพาะปลูก พื้นที่เก็บเกี่ยว ผลผลิต และผลผลิตเฉลี่ย
- แยกตัวชี้วัดต่างหน่วยออกจากกัน ไม่ใช้แกนเดียวร่วมกันระหว่างไร่ ตัน และกิโลกรัมต่อไร่
- เพิ่มกราฟเปรียบเทียบรายอำเภอระหว่างปีที่เลือกกับปีก่อน
- เพิ่มกราฟสัดส่วนพื้นที่รายพืช และเปลี่ยนเป็นรายอำเภอเมื่อเลือกพืช
- เพิ่มตารางสรุปรายปีพร้อมอัตราเปลี่ยนแปลงจากปีก่อน
- เพิ่มตารางรายละเอียดและสถานะคุณภาพข้อมูล
- แสดงเวลาที่ดึงข้อมูลและสถานะแหล่งข้อมูล
- ปรับ responsive layout สำหรับโทรศัพท์ แท็บเล็ต และคอมพิวเตอร์

## ไฟล์หลักที่เปลี่ยน

- `apps/web/src/main.tsx`
- `apps/web/src/styles.css`
- `README.md`

## ผลการตรวจสอบ

- Shared unit tests: ผ่าน 3 รายการ
- Apps Script tests: ผ่าน
- TypeScript typecheck: ผ่าน
- Vite production build: ผ่าน

## การนำขึ้นใช้งาน

1. แตกไฟล์ ZIP
2. นำไฟล์ทั้งหมดขึ้น Repository `Krit-PJ/KPP_Agri-Dashboard`
3. Commit และ push ไปยัง branch `main`
4. ตรวจ GitHub Actions ให้ขั้น `test-build` และ `deploy` ผ่าน
5. เปิด `https://krit-pj.github.io/KPP_Agri-Dashboard/`

Google Apps Script ใน `google-apps-script` ยังคงใช้กับ Google Sheet เดิมได้
ไม่ต้อง Deploy เป็น Web App และไม่ต้องเปลี่ยน URL ฐานข้อมูล
## รุ่น 4.0.0 — Multi-year Production Situation

- เปลี่ยนตัวกรองปีเป็น Card ใต้ Header และเลือกปีแบบค่าเดียว/หลายค่า/ข้ามปีได้
- เปลี่ยนหัวข้อ “ตัวชี้วัดการผลิต” เป็น “สถานการณ์การผลิต”
- เพิ่มแถบ Bullet ด้านซ้ายสำหรับพืช 9 ชนิด
- แยกกราฟเนื้อที่เพาะปลูกเป็น Line Graph และผลผลิตเป็น Bar Graph
- เพิ่ม Donut Graph สัดส่วนเนื้อที่เพาะปลูกของพืชทุกชนิด แยกตามปีที่เลือก
- เพิ่มตารางเนื้อที่ปลูก เนื้อที่เก็บเกี่ยว ผลผลิต ผลผลิตเฉลี่ย และร้อยละเปรียบเทียบ
- การเปรียบเทียบร้อยละอ้างอิงปีก่อนหน้าตามลำดับข้อมูลจริง แม้เลือกปีแบบข้ามปี
- รองรับ Desktop, Tablet และ Mobile

## รุ่น 4.1.0 — Verified Requirements

- แก้แถบชนิดพืชจาก 10 ปุ่มให้เป็น Bullet พืช 9 แถบตามข้อกำหนด
- กำหนดรายการพืช 9 ชนิดอย่างชัดเจน แม้ข้อมูลบางปีของพืชบางชนิดยังไม่มีระเบียน
- เพิ่มฟังก์ชันทดสอบการเลือกปีแบบข้ามปีและการรักษาปีที่เลือกอย่างน้อย 1 ปี
- เพิ่มฟังก์ชันและการทดสอบร้อยละ Donut โดยใช้ผลรวมพื้นที่ของพืชทุกชนิดเป็นฐาน
- เพิ่มป้าย `Dashboard v4.1.0` เพื่อยืนยันรุ่นที่ถูก Deploy บน GitHub Pages
- เพิ่ม `docs/requirement-traceability-v4.1.md` สำหรับตรวจสอบข้อกำหนดทั้ง 6 ข้อ

## รุ่น 4.2.0 — Google Sheets Live Data Contract

- แก้การเชื่อมโยงรหัสพืชจาก Google Sheets `C01–C09` ให้ตรงกับรหัสภายในของ Dashboard
- แปลงชื่อพืชเป็นชื่อมาตรฐานก่อนนำไปกรอง คำนวณ KPI และสร้างกราฟ
- รองรับสถานะจากชีต เช่น `active`, `valid`, `warning` และแปลงเป็น Data Contract ของระบบ
- เปลี่ยน cache key เป็น `v3` และตรวจรหัสพืชใน cache เพื่อไม่ใช้ข้อมูลเก่าที่แปลงผิด
- ตรวจหลังอ่านข้อมูลสดว่ามีพืชที่เผยแพร่ครบทั้ง 9 ชนิด หากไม่ครบจะแจ้งข้อผิดพลาดและใช้ fallback
- เพิ่มสถานะแหล่งข้อมูล จำนวนระเบียน ช่วงปี เวลาที่ตรวจสอบ และปุ่มโหลดข้อมูลใหม่บน Dashboard
- เพิ่ม `npm run data:audit` สำหรับตรวจการเชื่อม Google Sheets และสรุปจำนวนระเบียน/ปี/พืช
- เพิ่ม unit tests สำหรับการแปลงรหัสพืชและสถานะ รวมเป็น 7 รายการ
- เพิ่ม `docs/google-sheets-live-verification-v4.2.md` สำหรับตรวจสอบการเชื่อมต่อและเงื่อนไขยอมรับงาน

## รุ่น 4.3.0 — District-by-year Bar Charts & Accessibility

- เปลี่ยนกราฟเนื้อที่เพาะปลูกเป็น Bar chart โดยใช้แกน X เป็นอำเภอ และแยกชุดแท่งตามปี พ.ศ. ที่เลือก
- ปรับกราฟผลผลิตให้ใช้เงื่อนไข “อำเภอ × ปี” แบบเดียวกัน รองรับการเลือกปีเดียว หลายปี และปีที่ไม่ต่อเนื่อง
- ให้ตัวกรองอำเภอควบคุมกราฟทั้งสองรายการ หากเลือกอำเภอเดียวจะแสดงเฉพาะอำเภอนั้น
- เพิ่มลูกศรขึ้นสีเขียวพร้อมเครื่องหมายบวก และลูกศรลงสีแดงพร้อมเครื่องหมายลบในตารางสถานการณ์การผลิตรายปี
- เพิ่มแถบช่วยการเข้าถึงด้านบนสำหรับปรับขนาดตัวอักษร `ก− / ก / ก+` พร้อมจดจำการตั้งค่าของผู้ใช้
- ขยายตัวอักษรในกราฟตามค่าที่ผู้ใช้เลือก และทำให้กราฟ 11 อำเภอบนมือถือเลื่อนแนวนอนได้
- เพิ่ม unit test การรวมข้อมูลรายอำเภอแยกตามปี รวมเป็น 8 รายการ
- เพิ่ม `docs/requirement-traceability-v4.3.md` สำหรับตรวจสอบข้อกำหนดและผลทดสอบรุ่นนี้
# KPP Agri Dashboard v4.4.0

- ปรับเครื่องมือขนาดตัวอักษรเป็น `ก / ก+ / ก++`
- คลิกแท่ง Bar chart เพื่อเลือกและ Active ข้อมูลเฉพาะปีนั้นทั้ง Dashboard
- เพิ่มปุ่มปีใต้กราฟสำหรับคีย์บอร์ดและอุปกรณ์สัมผัส
- เพิ่มส่วนเข้าสู่ Admin Data Manager บน Dashboard
- ปรับ Admin เป็นรุ่น 1.1.0 พร้อมสรุปจำนวนระเบียนและตัวกรองสถานะ
- คงระบบ CRUD, validation, duplicate protection, soft delete, restore, backup และ audit

## รุ่น 4.4.1 — Resilient Startup & Sticky Year Filter

- เปลี่ยนการเริ่มต้นหน้าเว็บให้แสดง Dashboard จาก snapshot ที่ฝังใน JavaScript bundle ทันที ก่อนอัปเดตข้อมูลสดจาก Google Sheets เบื้องหลัง
- ตัดการพึ่งพา path ของไฟล์ JSON ขณะเริ่มระบบ จึงไม่เกิดหน้าว่างเมื่อ deploy ใต้ GitHub Pages repository path
- หากข้อมูลสดล้มเหลว ระบบยังคงใช้งาน Dashboard ได้และแสดงสาเหตุพร้อมปุ่มโหลดข้อมูลใหม่
- ตรึงส่วนเลือกปี พ.ศ. ไว้ใต้เมนูหลักบน Desktop และ Mobile โดยไม่บังเนื้อหา
- เพิ่ม automated validation สำหรับ snapshot 792 ระเบียน พ.ศ. 2556–2566 ครบ 9 พืชและ 11 อำเภอ
- ยืนยันว่าเว็บไซต์ Production เดิมยังเป็น `v4.4.0`; ต้อง deploy รุ่นนี้จึงจะได้รับการแก้ไข

## รุ่น 4.4.2 — Security & Data Integrity

- อัปเกรด Apache ECharts เป็น `6.1.x` ซึ่งเป็นรุ่นที่แก้ช่องโหว่ XSS และถอด React Router ที่ไม่ได้ใช้งาน
- เพิ่ม runtime validation สำหรับข้อมูลสด, cache และ snapshot ก่อนนำไปคำนวณหรือแสดงผล
- ป้องกัน `record_id` ซ้ำ ปี พ.ศ./ค.ศ. ไม่สอดคล้อง ค่าติดลบ ตัวเลขเสียรูป และ CSV ที่ปิด quote ไม่ครบ
- เปลี่ยนสถานะคุณภาพที่ไม่รู้จักให้เป็น `warning` เพื่อไม่ตีความข้อมูลผิดเป็นผ่านโดยอัตโนมัติ
- ปรับตัวกรองกลับสู่ปีและอำเภอที่มีอยู่จริงเมื่อแหล่งข้อมูลเปลี่ยน
- เพิ่ม unit tests ฝั่งเว็บ 5 รายการ รวมทั้งระบบเป็น 14 รายการ
- เพิ่ม Dependabot สำหรับ npm และ GitHub Actions และเพิ่ม API typecheck ใน CI

## รุ่น 4.4.3 — React Hooks Production Fix

- แก้หน้าเว็บ production ว่างจาก React error #310 (`Rendered more hooks than during the previous render`)
- ย้าย `useCallback` ให้อยู่ก่อน conditional return เพื่อให้ลำดับ Hooks คงที่ทุก render
- เปลี่ยนชื่อ ECharts `use` เป็น `registerECharts` เพื่อไม่ให้ตัวตรวจ React Hooks ตีความผิด
- เพิ่ม ESLint และ `eslint-plugin-react-hooks` ใน CI เพื่อหยุด deployment เมื่อพบการใช้ Hooks ผิดกฎ

## รุ่น 4.4.4 — Crop-aware Year Selection

- ยืนยันการเชื่อม Google Sheets สำเร็จ 792 ระเบียน และพบว่าสาเหตุ KPI เป็นศูนย์คือพืชแต่ละชนิดมีช่วงปีข้อมูลไม่เท่ากัน
- เลือกปีล่าสุดที่มีข้อมูลของพืชปัจจุบันโดยอัตโนมัติเมื่อเริ่มระบบหรือเปลี่ยนชนิดพืช
- ปิดปุ่มปีที่พืชปัจจุบันไม่มีระเบียน พร้อมคำอธิบายเมื่อชี้เมาส์
- ปรับปุ่ม “เลือกทุกปี” และ “เฉพาะปีล่าสุด” ให้ใช้เฉพาะปีที่มีข้อมูลจริง
- เพิ่ม unit test สำหรับการหาปีตามชนิดพืชและการปรับตัวเลือกปี
